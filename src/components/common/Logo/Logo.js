import { LogoName, LogoIcon, LogoContainer } from './Logo.style';

const Logo = () => {
  return (
    <LogoContainer>
      <LogoIcon />
      <LogoName>Wallet</LogoName>
    </LogoContainer>
  );
};

export default Logo;
